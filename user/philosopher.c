#include "philosopher.h"


void main_philosopher() {
    write( STDOUT_FILENO, "Philospher Started...\n", 22 );
    int count = 0;
    while( 1 ) {
        count++;
    }

    exit( EXIT_SUCCESS );
}
