#include "philosopher.h"


void main_philosopher2() {
    write( STDOUT_FILENO, "Philospher Started...\n", 22 );
    int id;
    int count = 0;
    //int fd = open("My first pipe", 0666);
    while( 1 ) {
        count++;
    }

    exit( EXIT_SUCCESS );
}
